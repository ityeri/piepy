services:
  app:
    build: .
    image: piepy-image
    container_name: piepy-app
    tty: true

    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      LOG_FILE: ${LOG_FILE}
      DOWNLOAD_DIR: ${DOWNLOAD_DIR}

    env_file:
      - .env
