services:
  app:
    build: .
    image: piepyimage
    container_name: piepyapp
    tty: true

    environment:
      BOT_TOKEN: ${BOT_TOKEN}

    env_file:
      - .env
    depends_on:
      - db

volumes:
  dbdata:
